export const metadata = {
  title: "Package Scale Reference | Adam Bechtold",
  description:
    "Interactive visualization comparing package dimensions with a 6-foot human reference, all to scale.",
};

/**
 * Layout component for the package scale reference page.
 * @param {object} props - Component props
 * @param {React.ReactNode} props.children - Child components to render
 * @return {React.ReactNode} The children components
 */
export default function PackagesLayout({ children }) {
  return children;
}
